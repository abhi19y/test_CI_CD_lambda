exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello from UpdatedLambda,DIFIED. testin checkgit commit --amend --reset-authorg.!'),
    };
    return response;
};
