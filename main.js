exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('FINAL TESTING-> LAMBDA with AWS CLI'),
    };
    return response;
};
