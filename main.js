exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('FINAL TESTING --- testing on 4th Jan CHECKING AGAIN--> LAMBDA with AWS CLI'),
    };
    return response;
};
