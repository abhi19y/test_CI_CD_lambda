exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello NodeJS App , added aws cli cmd on jenkins TESTING LAMBDA JENKINS WEBHOOK'),
    };
    return response;
};
